<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<html>
<head>
    <title>Apps</title>

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: radial-gradient(circle at top, #1e293b 0, #020617 45%, #000 100%);
            margin: 0;
            color: #e5e7eb;
        }

        .container {
            max-width: 960px;
            margin: 30px auto 40px;
            padding: 0 18px;
        }

        /* === HERO CARD (Welcome to App Store) === */
        .hero-card {
            background: linear-gradient(135deg, #1e3a8a, #2563eb);
            border-radius: 20px;
            padding: 20px 22px 18px;
            box-shadow: 0 22px 45px rgba(15,23,42,0.95);
            border: 1px solid rgba(148,163,184,0.5);
            margin-bottom: 24px;
        }

        .hero-title {
            font-size: 26px;
            font-weight: 600;
            margin: 0 0 6px;
        }

        .hero-title span {
            color: #f97316;
        }

        .hero-text {
            font-size: 13px;
            color: #e5e7eb;
            margin-bottom: 18px;
            max-width: 650px;
        }

        .hero-actions {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 8px;
        }

        .btn-pill-primary,
        .btn-pill-outline {
            border-radius: 999px;
            padding: 9px 16px;
            font-size: 13px;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            cursor: pointer;
            border: none;
            text-decoration: none;
        }

        .btn-pill-primary {
            background: linear-gradient(135deg,#f97316,#ec4899);
            color: #f9fafb;
            font-weight: 600;
        }

        .btn-pill-outline {
            background: transparent;
            border: 1px solid rgba(226,232,240,0.7);
            color: #e5e7eb;
        }

        .hero-tip {
            font-size: 11px;
            color: #cbd5f5;
            margin-top: 4px;
        }

        /* heading */
        .page-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 18px;
        }

        .page-title {
            font-size: 22px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .page-title i {
            color: #f97316;
        }

        /* search bar */
        .search-box {
            background: rgba(15,23,42,0.96);
            padding: 12px 14px;
            border-radius: 14px;
            border: 1px solid rgba(148,163,184,0.45);
            box-shadow: 0 14px 30px rgba(15,23,42,0.9);
            margin-bottom: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .search-box input {
            flex: 1;
            padding: 8px 10px;
            border-radius: 10px;
            border: 1px solid #4b5563;
            background: #020617;
            color: #e5e7eb;
            font-size: 13px;
        }

        .search-box input:focus {
            outline: none;
            border-color: #6366f1;
            box-shadow: 0 0 0 1px #6366f1;
        }

        .btn-search {
            border: none;
            border-radius: 999px;
            padding: 8px 14px;
            font-size: 13px;
            cursor: pointer;
            background: linear-gradient(135deg,#6366f1,#f97316);
            color: #f9fafb;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        /* error message */
        .error-msg {
            margin-bottom: 12px;
            padding: 8px 10px;
            border-radius: 10px;
            font-size: 13px;
            background: rgba(248,113,113,0.14);
            border: 1px solid rgba(248,113,113,0.8);
            color: #fecaca;
        }

        /* app cards */
        .app-card {
            background: rgba(15,23,42,0.97);
            border-radius: 16px;
            padding: 14px 16px 12px;
            border: 1px solid rgba(148,163,184,0.40);
            box-shadow: 0 16px 40px rgba(15,23,42,0.95);
            margin-bottom: 14px;
        }

        .app-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 6px;
        }

        .app-name {
            font-size: 16px;
            font-weight: 600;
        }

        .rating-pill {
            padding: 3px 8px;
            border-radius: 999px;
            font-size: 11px;
            background: rgba(250,204,21,0.16);
            border: 1px solid rgba(250,204,21,0.8);
            color: #facc15;
        }

        .app-meta {
            font-size: 12px;
            color: #9ca3af;
            margin-bottom: 6px;
        }

        .app-desc {
            font-size: 13px;
            color: #e5e7eb;
        }
    </style>
</head>
<body>

<jsp:include page="navbar.jsp" />

<div class="container">

    <!-- 🔹 Hero card on top -->
    <div class="hero-card">
        <div class="hero-title">
            Welcome to <span>App Store</span>
        </div>
        <div class="hero-text">
            Manage users, apps and owners from a single place. Login as a user, register your own apps as owner,
            or control everything as admin.
        </div>

        <div class="hero-actions">
            <a href="${pageContext.request.contextPath}/users-ui/login" class="btn-pill-primary">
                <i class="fa-solid fa-user"></i> User Portal
            </a>
            <a href="${pageContext.request.contextPath}/owners-ui/login" class="btn-pill-outline">
                <i class="fa-solid fa-user-tie"></i> Owner Portal
            </a>
            <a href="${pageContext.request.contextPath}/admins-ui/login" class="btn-pill-outline">
                <i class="fa-solid fa-shield-halved"></i> Admin Login
            </a>
        </div>

        <div class="hero-tip">
            Tip: New here? User / Owner portals se aap registration screens pe ja sakte hain.
        </div>
    </div>

    <!-- Page heading -->
    <div class="page-header">
        <div class="page-title">
            <i class="fa-solid fa-grid-2"></i>
            <span>Apps</span>
        </div>
    </div>

    <!-- Search -->
    <form action="${pageContext.request.contextPath}/apps/genre" method="get" class="search-box">
        <i class="fa-solid fa-magnifying-glass"></i>
        <input name="genre" placeholder="Search by genre (e.g. Tools, Games, Social)"/>
        <button type="submit" class="btn-search">
            <i class="fa-solid fa-search"></i> Search
        </button>
    </form>

    <c:if test="${not empty error}">
        <div class="error-msg">${error}</div>
    </c:if>

    <!-- App cards -->
    <c:forEach var="app" items="${apps}">
        <div class="app-card">
            <div class="app-header">
                <div class="app-name">${app.appName}</div>
                <div class="rating-pill">⭐ ${app.rating}</div>
            </div>
            <div class="app-meta">
                Genre: ${app.genre} &nbsp;|&nbsp;
                Type: ${app.appType}
            </div>
            <div class="app-desc">
                ${app.description}
            </div>
        </div>
    </c:forEach>

</div>

</body>
</html>
