<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<html>
<head>
    <title>Admin Dashboard</title>

    <!-- Google Fonts + Icons CDN -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: #f3f5f8;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 95%;
            margin: auto;
            padding: 20px;
        }
        .welcome-box {
            background: #ffffff;
            padding: 15px 20px;
            border-radius: 10px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
            font-size: 18px;
            margin-bottom: 20px;
        }
        .welcome-box span {
            font-weight: 600;
            color: #ff5722;
        }

        /* ==== DASHBOARD CARDS ==== */
        .dashboard-cards {
            display: flex;
            justify-content: space-between;
            gap: 20px;
            margin-bottom: 25px;
        }
        .card {
            flex: 1;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            cursor: pointer;
            text-align: center;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
            transition: 0.25s ease;
        }
        .card:hover {
            transform: translateY(-4px);
            box-shadow: 0 4px 18px rgba(0,0,0,0.12);
        }
        .card i {
            font-size: 35px;
            color: #ff5722;
            margin-bottom: 10px;
        }
        .card h3 {
            font-size: 18px;
            margin-bottom: 5px;
            font-weight: 600;
        }
        .card p {
            font-size: 13px;
            color: #777;
        }
        .card-active {
            border: 2px solid #ff9800;
        }

        /* === SECTION STYLE === */
        .section-title {
            margin-top: 10px;
            background: #ffcc80;
            padding: 6px 10px;
            border-radius: 4px;
            font-weight: 600;
        }

        table {
            border-collapse: collapse;
            width: 100%;
            background: white;
            font-size: 14px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }
        th, td {
            border: 1px solid #eee;
            padding: 8px;
        }
        th {
            background: #ffe0b2;
        }

        .btn-delete, .btn-activate {
            padding: 4px 8px;
            border-radius: 4px;
            border: none;
            cursor: pointer;
            font-size: 12px;
            color: white;
        }
        .btn-delete{
            background: #e53935;
        }
        .btn-activate {
            background: #43a047;
        }

        .badge-active {
            background: #43a047;
            color: white;
            padding: 3px 6px;
            border-radius: 4px;
        }
        .badge-inactive {
            background: #e53935;
            color: white;
            padding: 3px 6px;
            border-radius: 4px;
        }

        /* Sections hidden by default */
        .section-panel {
            display: none;
            margin-bottom: 25px;
        }
    </style>

    <script>
        // ek hi place pe state manage
        function showSection(section) {
            const sections = ['users', 'apps', 'owners'];

            sections.forEach(s => {
                const panel = document.getElementById(s + '-panel');
                const card  = document.getElementById(s + '-card');
                if (panel) {
                    panel.style.display = (s === section) ? 'block' : 'none';
                }
                if (card) {
                    if (s === section) card.classList.add('card-active');
                    else card.classList.remove('card-active');
                }
            });
        }

        // page load pe koi table nahi dikhana ho to kuch mat call karo
        // agar Users default dikhana ho to:
        // window.onload = function() { showSection('users'); };
    </script>
</head>
<body>

<jsp:include page="navbar.jsp"/>

<div class="container">

    <!-- WELCOME MESSAGE -->
    <div class="welcome-box">
        Hello, <span>${sessionScope.userName}</span> 👋  
        Welcome to your dashboard
    </div>

    <!-- CARDS / TABS -->
    <div class="dashboard-cards">
        <div class="card" id="users-card" onclick="showSection('users')">
            <i class="fa-solid fa-users"></i>
            <h3>Users</h3>
            <p>Manage User Accounts</p>
        </div>

        <div class="card" id="apps-card" onclick="showSection('apps')">
            <i class="fa-solid fa-mobile-screen"></i>
            <h3>Apps</h3>
            <p>Manage App Records</p>
        </div>

        <div class="card" id="owners-card" onclick="showSection('owners')">
            <i class="fa-solid fa-user-tie"></i>
            <h3>App Owners</h3>
            <p>Manage Owner Accounts</p>
        </div>
    </div>

    <!-- USERS SECTION (hidden until card click) -->
    <div id="users-panel" class="section-panel">
        <div class="section-title">Users</div>
        <table>
            <tr>
                <th>ID</th><th>Name</th><th>Username</th><th>Email</th><th>Mobile</th><th>Status</th><th>Actions</th>
            </tr>
            <c:forEach var="u" items="${users}">
                <tr>
                    <td>${u.userId}</td>
                    <td>${u.firstName} ${u.lastName}</td>
                    <td>${u.userName}</td>
                    <td>${u.email}</td>
                    <td>${u.mobileNo}</td>
                    <td>
                        <c:if test="${u.status == 'ACTIVE'}">
                            <span class="badge-active">ACTIVE</span>
                        </c:if>
                        <c:if test="${u.status == 'INACTIVE'}">
                            <span class="badge-inactive">INACTIVE</span>
                        </c:if>
                    </td>
                    <td>
                        <form action="${pageContext.request.contextPath}/admins-ui/users/${u.userId}/delete"
                              method="post" style="display:inline">
                            <button type="submit" class="btn-delete">Delete</button>
                        </form>
                        <c:if test="${u.status == 'INACTIVE'}">
                            <form action="${pageContext.request.contextPath}/admins-ui/users/${u.userId}/activate"
                                  method="post" style="display:inline">
                                <button type="submit" class="btn-activate">Activate</button>
                            </form>
                        </c:if>
                    </td>
                </tr>
            </c:forEach>
        </table>
    </div>

    <!-- APPS SECTION -->
    <div id="apps-panel" class="section-panel">
        <div class="section-title">Apps</div>
        <table>
            <tr>
                <th>ID</th><th>Name</th><th>Owner ID</th><th>Genre</th><th>Type</th><th>Rating</th><th>Actions</th>
            </tr>
            <c:forEach var="a" items="${apps}">
                <tr>
                    <td>${a.appId}</td>
                    <td>${a.appName}</td>
                    <td>${a.appOwnerId}</td>
                    <td>${a.genre}</td>
                    <td>${a.appType}</td>
                    <td>${a.rating}</td>
                    <td>
                        <form action="${pageContext.request.contextPath}/admins-ui/apps/${a.appId}/delete"
                              method="post" style="display:inline">
                            <button type="submit" class="btn-delete">Delete</button>
                        </form>
                    </td>
                </tr>
            </c:forEach>
        </table>
    </div>

    <!-- OWNERS SECTION -->
    <div id="owners-panel" class="section-panel">
        <div class="section-title">App Owners</div>
        <table>
            <tr>
                <th>ID</th><th>Name</th><th>Username</th><th>Email</th><th>Mobile</th><th>Actions</th>
            </tr>
            <c:forEach var="owner" items="${owners}">
                <tr>
                    <td>${owner.appOwnerId}</td>
                    <td>${owner.firstName} ${owner.lastName}</td>
                    <td>${owner.userName}</td>
                    <td>${owner.email}</td>
                    <td>${owner.mobile}</td>
                    <td>
                        <form action="${pageContext.request.contextPath}/admins-ui/owners/${owner.appOwnerId}/delete"
                              method="post" style="display:inline">
                            <button type="submit" class="btn-delete">Delete</button>
                        </form>
                    </td>
                </tr>
            </c:forEach>
        </table>
    </div>

</div>
</body>
</html>
